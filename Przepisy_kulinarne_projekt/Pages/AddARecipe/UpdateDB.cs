using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Przepisy_kulinarne_projekt.Pages.AddARecipe
{
    public class UpdateDB
    {
        /*public int UpdateOcena(Przepisy_kulinarne_projekt.Data.ApplicationDbContext context)
        {
            var przepis = context.Recipes.Where(x => x.Id ==).SingleOrDefault();
            przepis.Rating = ocena;
            context.SaveChanges();
        }*/
    }
}
